# Multi-ACCDOA：单次最小预检完成

## Material Passport

- Origin Skill：academic-research-suite / experiment-agent，run。
- 日期：2026-09-10，Asia/Shanghai。
- 状态：本次有界预检COMPLETED；两条录音所测项目通过，不代表全量实验或所有边界已经验证。
- 执行授权：用户对一次30分钟、无优化器预检回复“可以”；未扩展为全量缓存、训练或evaluation授权。

## 结果先行

一次运行，退出码0，无超时、无重试。守护记录进程用时48.656秒；脚本内部检查用时21.542秒，两者差别包含解释器/依赖启动等开销。全部95条记录通过，其中包含来源/文件hash检查，不能解读为95项独立科学实验。

仅rabbit02的4号RTX3090，GPU UUID见[启动回执](artifacts/LAUNCH.json)。0–3号仍由其他任务占用，没有共享或停止其任务。结束后4号卡恢复0MiB。PyTorch记录最大张量分配127882752字节（约122MiB），不是nvidia-smi总进程显存峰值。

**没有创建优化器，没有更新任何主干或小头参数，没有启动三个小头训练，没有导出全量缓存，没有进行新evaluation推理或评分。** evaluation仅封存已有特征文件名目录清单，未读取其特征内容；历史manifest所含已暴露指标不是新盲测结果。

## 已通过内容

| 检查 | 结果和边界 |
|---|---|
| 原运行时 | Python 3.8.11 / PyTorch 1.10.0+cu111加载指定权重成功 |
| 原生chunk/隐藏维度 | 两条输入均12×7×250×64；隐藏12×50×128；输出12×50×126 |
| hook透明性 | 不加hook与加hook的完整原输出逐位一致 |
| 主干冻结 | requires_grad全部关闭后原输出逐位一致；参数/缓冲区内容hash不变，无主干梯度 |
| 三轨道解码 | 新事件适配与封存历史阈值解码器一致；与原训练入口的原生解码CSV逐字节一致 |
| 历史validation样本 | 最佳权重重新评分目录中的指定一条CSV逐字节复现；不是与训练最后轮目录比较 |
| 三种零头 | 两条录音上的原模型＋方向修正、＋历史、＋运动监督：零修正和原xyz逐位一致，CSV逐字节一致 |
| 小头梯度 | 三种小头在两条录音上均有有限、非零总梯度，反向后参数hash不变；未执行优化步骤 |
| 前缀 | 每chunk后125个声学帧替换后，前25个标签帧原输出差为0；测试对应2.5秒可用时间 |
| 原前端 | 原波形2.5秒后替换，前125个声学帧复数谱逐位一致；nfft1024、左填充544样本 |
| chunk独立性 | 同批其他chunk改变，首chunk输出逐位一致 |
| 合成边界 | 无合并/三种双轨合并/三轨合并、合并范数低于0.5仍保留、空帧、轨道切换、跨chunk断开、零范数回退、近并列排除、无pair零损失/梯度、修正范数保持通过 |
| 来源不变 | 封存源码逐项核对；两条输入、GT、历史参照文件运行后hash不变 |

CSV恒等也保持对应事件数、非方向字段和重复条目；本阶段未另行运行全量纯SED评分，不能写作全validation检测保持已通过。未伪造独立SED概率张量。

两条录音均完整60秒、12个原生5秒chunk，不改变分块。小头特征来自末层线性层输入，同帧事件共享128维特征，以原方向区分。

## 固定样本的监督分母

| 录音 | 原事件 | 固定匹配 | 未匹配预测 | GT事件 | 原始预测关联 | 有效motion pair | 非零位移坐标数 |
|---|---:|---:|---:|---:|---:|---:|---:|
| train：fold2_room1_mix001_ov1 | 384 | 382 | 2 | 484 | 371 | 371 | 316 |
| validation：fold1_room1_mix001_ov1 | 389 | 319 | 70 | 380 | 365 | 300 | 291 |

这些是预检分母，不是新增实验成绩；没有按表现选样本，也没有用GT决定推理关联。零头初始化检查实例不计作完成了小头训练。

## 尚未完成的门槛

1. 完整train500/validation100隐藏缓存及来源manifest；本次只生成两条预检输出，不能直接声称缓存就绪。
2. 全validation历史CSV及统一官方评分回归、全量检测保持/纯SED计数与固定匹配诊断。
3. 非完整原生chunk的padding专项测试；本次两条均整60秒。前缀测试覆盖固定切点，不是对任意输入/切点的数学证明。
4. 非零/训练后小头的最终输出前缀测试及所有缓存边界核验；本次主干前缀测试与零头恒等不能替代它。
5. 全量训练入口、两帧平滑/卡尔曼事件适配与完整评分入口尚未交付为可直接开跑的流水线。既定方法与参数不变，不以赶时间跳过这些实现和检查。
6. 三个独立小头的40–200轮双平台训练、validation-best封存，以及全冻结后的六条件evaluation，均未执行。

## 后续预算（仍需授权）

沿用[预算登记](artifacts/MULTI_ACCDOA_PREFLIGHT_PLAN_20260910.md)：train五个fold各100条、validation100条，各任务30分钟硬限；全缓存预检30分钟；三个小头各4小时，合计最多12 GPU·小时。之后evaluation分两批各100条、每批30分钟，评分30分钟；仅在三个头均冻结后进行。

本次两条录音的各项前向、解码、梯度和前端检查分别约1.98秒/0.82秒，未发现必须提高30分钟分批硬限的证据，但这不是全缓存吞吐测试或完整耗时承诺。源快照、全文件hash和评分可能另有开销。仍只有一张空闲卡时串行；每次启动重新检查资源，不使用其他任务占用的卡。

下一阶段建议范围：补齐上述适配和边界检查，生成全量train/validation缓存；全部回归通过才训练三个独立头。任何不一致保留证据并停止，不自动重试、放宽门槛或扩大方法。授权范围须由用户另行确认。

## 来源与交付

- [授权记录](artifacts/operations/AUTHORIZATION.md)、[原始检查日志](artifacts/checks.jsonl)、[预检总回执](artifacts/PREFLIGHT.json)、[停止回执](artifacts/EXIT.json)。
- [适配实现](artifacts/operations/event_adapter.py)、[本次预检实现](artifacts/operations/preflight.py)、[封存与超时守护](artifacts/operations/prepare_and_launch.py)。
- [完整来源清单](artifacts/SOURCE.json)：SHA256 `710a6bb4d6da01b61afed92d401b06a88580d90122130c9b56a38b46cb494d72`。
- 预检总回执SHA256 `95002b485ad4c391ab929184f0f94ace8e4068b4503af07c4e6a4edbd788ce35`。
- validation样本原CSV及历史CSV同hash：`e39f6676a7205ded36fb7d213ad5b9c5827615edaf29f8e3766d784d8b1a8c86`。
- 轻量归档SHA256 `25c586c64cfb710e17c5adb6babba46e113b80413b64fb48a7504c2bce6f2446`，远端与本地一致；归档展开目录发布，压缩包不上传仓库。
- 权重、scaler本体及两条浮点NPZ保留于rabbit02新目录，未上传GitHub；SOURCE.json所列两份二进制因此在轻量交付中有意缺席，并非来源丢失。音频和GT原文件没有复制到发布仓库。

本次按academic-research-suite区分了已测范围、未测范围和执行授权，没有将两条预检提升为全量复现结论。既有EINV2记录和历史Multi结果未覆盖。
