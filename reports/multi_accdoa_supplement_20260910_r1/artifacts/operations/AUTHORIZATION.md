# 单次最小预检授权

日期2026-09-10。用户在只读核查和预算登记之后，对“仅实现最小适配，并在空闲3090上执行一次30分钟硬限、无优化器的预检，通过后再确认全量缓存和三个小头训练”明确回复“可以”。

授权范围：独立目录下适配实现、源码与输入封存、固定train `fold2_room1_mix001_ov1` 和validation `fold1_room1_mix001_ov1` 两条完整录音，合成边界、原生/冻结/hook输出回归与零头梯度检查。随机种子2026；不创建优化器、不更新权重；不访问evaluation内容。

仅rabbit02真正空闲RTX3090；拟用4号卡，启动前重新检查空闲，以UUID绑定。0–3号卡已被他人占用，不共享、不终止其进程。RB05不计算。

一次预检1800秒硬限，超限仅结束自身进程组，失败不自动重跑。启动命令由已授权适配实现生成：历史Python环境执行 `operations/preflight.py`，由 `prepare_and_launch.py dispatch` 启动一次持久守护；LAUNCH.json封存完整命令和SOURCE.json的hash。首次运行前封存上传代码字节，运行失败后不修补原快照。

没有获得全量缓存、三个小头训练、新evaluation、扩预算、改阈值或失败重试授权。本次交付后另行确认。
